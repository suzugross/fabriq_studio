<UserControl x:Class="FabriqStudio.Views.ModuleEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- タイトル -->
        <TextBlock Grid.Row="0"
                   Text="モジュール編集"
                   FontSize="20" FontWeight="Bold"
                   Foreground="#2D2D30"
                   Margin="0,0,0,4" />

        <TextBlock Grid.Row="0"
                   Text="kernel/csv/categories.csv"
                   FontSize="11" Foreground="#888888"
                   VerticalAlignment="Bottom"
                   HorizontalAlignment="Right" />

        <!-- エラー表示 -->
        <TextBlock Grid.Row="1"
                   Text="{Binding ErrorMessage}"
                   Foreground="Red"
                   Margin="0,8,0,8"
                   Visibility="{Binding ErrorMessage,
                       Converter={StaticResource NullToCollapsedConverter}}" />

        <!-- ローディング -->
        <TextBlock Grid.Row="1"
                   Text="読み込み中..."
                   Foreground="#888888"
                   Margin="0,8,0,8"
                   Visibility="{Binding IsLoading,
                       Converter={StaticResource BoolToVisibilityConverter}}" />

        <!-- データグリッド -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Categories}"
                  IsReadOnly="True"
                  AutoGenerateColumns="False"
                  CanUserReorderColumns="True"
                  CanUserResizeColumns="True"
                  GridLinesVisibility="Horizontal"
                  AlternatingRowBackground="#F7F7F7"
                  BorderBrush="#DDDDDD" BorderThickness="1"
                  Margin="0,8,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="カテゴリ"  Binding="{Binding Category}" Width="200" />
                <DataGridTextColumn Header="表示順"    Binding="{Binding Order}"    Width="80" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
