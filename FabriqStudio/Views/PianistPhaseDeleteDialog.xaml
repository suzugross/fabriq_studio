<Window x:Class="FabriqStudio.Views.PianistPhaseDeleteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Phase 削除"
        Width="500"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="#F5F5F5">

    <Window.Resources>
        <Style x:Key="DlgButtonStyle" TargetType="Button">
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor"          Value="Hand" />
            <Setter Property="FontSize"        Value="13" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.85" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.70" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="22,18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="14"   />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="14"   />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="14"   />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   x:Name="HeaderText"
                   FontSize="14" FontWeight="Bold"
                   Foreground="#222222" />

        <TextBlock Grid.Row="2"
                   x:Name="DetailText"
                   FontSize="12" Foreground="#444444"
                   TextWrapping="Wrap" />

        <!-- CheckBox + 注釈を 1 つのスタックにまとめて Auto 行（Row=4）に乗せる
             （以前は注釈を 14px 固定行に置いて見切れていた） -->
        <StackPanel Grid.Row="4">
            <CheckBox x:Name="DeleteFileCheck"
                      IsChecked="False"
                      Foreground="#444444">
                <TextBlock x:Name="DeleteFileLabel" Text="instructions/.txt も削除する" />
            </CheckBox>
            <TextBlock FontSize="10" Foreground="#888888"
                       TextWrapping="Wrap"
                       Margin="22,4,0,0"
                       Text="（既定 OFF — 削除した Phase ID をあとで再作成したい場合の保険として保守的に残します。OFF のまま削除すると孤児ファイルになりますが、pianist 実行時は無害です）" />
        </StackPanel>

        <StackPanel Grid.Row="6"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="CancelBtn"
                    Style="{StaticResource DlgButtonStyle}"
                    Content="キャンセル"
                    Background="#E0E0E0"
                    Foreground="#333333"
                    Padding="16,6"
                    IsCancel="True"
                    Click="CancelBtn_Click" />
            <Button x:Name="OkBtn"
                    Style="{StaticResource DlgButtonStyle}"
                    Content="削除"
                    Background="#C85000"
                    Foreground="#FFFFFF"
                    Padding="16,6"
                    Margin="8,0,0,0"
                    IsDefault="True"
                    Click="OkBtn_Click" />
        </StackPanel>
    </Grid>
</Window>
