<Window x:Class="FabriqStudio.Views.PickListDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Width="700" Height="560"
        MinWidth="520" MinHeight="380"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="#F5F5F5">

    <Window.Resources>
        <Style x:Key="DlgButton" TargetType="Button">
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor"          Value="Hand" />
            <Setter Property="FontSize"        Value="13" />
            <Setter Property="Padding"         Value="18,6" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Bd" Property="Opacity" Value="0.85" /></Trigger>
                            <Trigger Property="IsPressed"   Value="True"><Setter TargetName="Bd" Property="Opacity" Value="0.70" /></Trigger>
                            <Trigger Property="IsEnabled"   Value="False"><Setter TargetName="Bd" Property="Opacity" Value="0.35" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="18,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 入力欄（検索 or 絞り込み） -->
        <DockPanel Grid.Row="0" Margin="0,0,0,10">
            <Button DockPanel.Dock="Right" Content="検索" Style="{StaticResource DlgButton}"
                    Background="#007ACC" Foreground="White" Margin="8,0,0,0"
                    Command="{Binding SearchCommand}"
                    Visibility="{Binding HasSearch, Converter={StaticResource BoolToVisibilityConverter}}" />
            <Grid>
                <TextBox x:Name="QueryBox" FontSize="13" Padding="8,6"
                         Text="{Binding Query, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding SearchCommand}" />
                    </TextBox.InputBindings>
                </TextBox>
                <TextBlock Text="{Binding InputHint}" Foreground="#999999" FontSize="12"
                           Margin="11,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=QueryBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </DockPanel>

        <!-- 一覧 -->
        <Grid Grid.Row="1">
            <ListView ItemsSource="{Binding Items}" FontSize="12.5"
                      Background="White" BorderBrush="#DDDDDD" BorderThickness="1"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="4,3" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsRegistered}" Value="True">
                                <Setter Property="Foreground" Value="#9E9E9E" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Width="34">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}" IsEnabled="{Binding CanCheck}"
                                              VerticalAlignment="Center" HorizontalAlignment="Center" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="名前" Width="260" DisplayMemberBinding="{Binding Name}" />
                        <GridViewColumn Header=""     Width="230" DisplayMemberBinding="{Binding Detail}" />
                        <GridViewColumn Header=""     Width="90"  DisplayMemberBinding="{Binding Detail2}" />
                        <GridViewColumn Header=""     Width="70"  DisplayMemberBinding="{Binding RegisteredText}" />
                    </GridView>
                </ListView.View>
            </ListView>
            <TextBlock Text="{Binding Message}" Foreground="#666666" FontSize="13"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Visibility="{Binding Message, Converter={StaticResource NullToCollapsedConverter}}" />
            <TextBlock Text="検索中…" Foreground="#666666" FontSize="13"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Visibility="{Binding IsSearching, Converter={StaticResource BoolToVisibilityConverter}}" />
        </Grid>

        <!-- フッター -->
        <DockPanel Grid.Row="2" Margin="0,12,0,0" LastChildFill="False">
            <TextBlock DockPanel.Dock="Left" VerticalAlignment="Center" FontSize="12.5" Foreground="#555555">
                <Run Text="{Binding SelectedCount, Mode=OneWay}" /><Run Text=" 件" />
            </TextBlock>
            <Button DockPanel.Dock="Right" Content="キャンセル" IsCancel="True" Style="{StaticResource DlgButton}"
                    Background="#E0E0E0" Foreground="#333333" Margin="8,0,0,0" />
            <Button DockPanel.Dock="Right" Content="{Binding ConfirmText}" Style="{StaticResource DlgButton}"
                    Background="#007ACC" Foreground="White" IsEnabled="{Binding CanConfirm}" Click="OnConfirm" />
        </DockPanel>
    </Grid>
</Window>
