<Window x:Class="FabriqStudio.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="fabriq studio"
        Height="700" Width="1100"
        MinHeight="500" MinWidth="800"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- ナビゲーションボタンのスタイル -->
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background"               Value="Transparent" />
            <Setter Property="Foreground"               Value="#BBBBBB" />
            <Setter Property="BorderThickness"          Value="0" />
            <Setter Property="Height"                   Value="44" />
            <Setter Property="FontSize"                 Value="13" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Padding"                  Value="16,0,0,0" />
            <Setter Property="Cursor"                   Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3E3E42" />
                                <Setter Property="Foreground" Value="#FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#007ACC" />
                                <Setter Property="Foreground" Value="#FFFFFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- ===== 左ペイン: ナビゲーション ===== -->
        <Border Grid.Column="0" Background="#252526">
            <DockPanel>

                <!-- アプリタイトル -->
                <Border DockPanel.Dock="Top"
                        Background="#1E1E1E"
                        Padding="16,14">
                    <StackPanel>
                        <TextBlock Text="fabriq studio"
                                   FontSize="16" FontWeight="Bold"
                                   Foreground="#FFFFFF" />
                        <TextBlock Text="管理ツール v0.1"
                                   FontSize="10" Foreground="#888888"
                                   Margin="0,2,0,0" />
                    </StackPanel>
                </Border>

                <!-- セクションラベル -->
                <TextBlock DockPanel.Dock="Top"
                           Text="メニュー"
                           FontSize="10" Foreground="#666666"
                           Margin="16,16,0,4"
                           FontWeight="SemiBold" />

                <!-- ナビゲーションボタン -->
                <StackPanel DockPanel.Dock="Top">
                    <Button Content="⚙  基本パラメータ"
                            Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateCommand}"
                            CommandParameter="BasicParams" />

                    <Button Content="🧩  モジュール編集"
                            Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateCommand}"
                            CommandParameter="ModuleEdit" />

                    <Button Content="🖥  端末一覧"
                            Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateCommand}"
                            CommandParameter="HostList" />
                </StackPanel>

                <!-- 下部スペーサー -->
                <Border DockPanel.Dock="Bottom"
                        Padding="16,12"
                        BorderThickness="0,1,0,0"
                        BorderBrush="#3E3E42">
                    <TextBlock Text="fabriq framework"
                               FontSize="10" Foreground="#555555" />
                </Border>

                <Grid /><!-- 残余スペースを埋める -->
            </DockPanel>
        </Border>

        <!-- ===== 右ペイン: コンテンツ ===== -->
        <ContentControl Grid.Column="1"
                        Content="{Binding CurrentPage}"
                        Background="White" />
    </Grid>
</Window>
